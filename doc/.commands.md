## Criar hash de senha

```bash
pnpm --filter @erp-360/api exec node --input-type=module -e "import bcrypt from 'bcryptjs'; console.log(await bcrypt.hash('suaSenha', 12))"
```

## Gerar de JWT

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```
